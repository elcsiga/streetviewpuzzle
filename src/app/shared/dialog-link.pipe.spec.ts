import { DialogLinkPipe } from './dialog-link.pipe';

describe('DialogLinkPipe', () => {
  it('create an instance', () => {
    const pipe = new DialogLinkPipe();
    expect(pipe).toBeTruthy();
  });
});
